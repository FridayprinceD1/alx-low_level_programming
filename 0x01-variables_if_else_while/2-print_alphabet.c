#include <stdlib.h>

#include <time.h>

#include <stdio.h>

/**
 * main -> assign a random nubit is executed
 * Return: always 0
 */

int main(void)
{
	int ch;

	for (ch = 97; ch <= 122; ch++)
	{
		putchar(ch);
	}
	putchar('\n'); /* ASCII CODE  for new line */

	return (0);
}
